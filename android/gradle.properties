org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

org.gradle.java.home=C:/JDK17_temp/jdk-17.0.14+7

# Compile Kotlin inside the Gradle daemon — the separate Kotlin daemon
# keeps crashing with "Daemon compilation failed: null" on this machine
# (same class of issue documented in CHANGELOG v1.2.0).
kotlin.compiler.execution.strategy=in-process
