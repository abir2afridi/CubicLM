You are fixing a production bug in CubicLM.

IMPORTANT:
This is NOT primarily a Preview bug.

React + Vite projects now preview correctly, but Next.js projects generated by CubicLM are being CORRUPTED before or during the file-writing pipeline.

You must find and fix the ACTUAL ROOT CAUSE.

DO NOT just modify the Preview component.
DO NOT add another workaround.
DO NOT convert Next.js to static HTML.
DO NOT manually repair one generated project.
DO NOT hardcode special cases for this specific portfolio.

The goal is to make CubicLM capable of reliably generating ANY valid Next.js project without corrupting source code.

========================================================

1. OBSERVED FAILURE
   ========================================================

A generated Next.js project contains:

package.json
next.config.js
app/layout.jsx
app/page.jsx
app/components/Navigation.jsx
app/components/About.jsx
app/components/Projects.jsx
app/components/Footer.jsx

package.json correctly contains:

"dev": "next dev"
"build": "next build"
"start": "next start"

and dependencies:

"next": "14.2.5"
"react": "18.3.1"
"react-dom": "18.3.1"

However, JSX source is corrupted.

Examples of observed corruption:

VALID:

return (
<> <Navigation /> <Hero /> <About />
</>
);

CORRUPTED:

return (
<>

</>
);

Another example:

VALID:

return (

  <html lang="en">
    <body>{children}</body>
  </html>
);

CORRUPTED:

return (

{children}

);

Another example:

VALID:

const id = `g-${a.slice(1)}-${b.slice(1)}`;

CORRUPTED:

const id = g-${a.slice(1)}-${b.slice(1)};

Another example:

VALID:

href="#contact"

CORRUPTED:

http://127.0.0.1:<random-port>/%22#contact%22

These are NOT valid transformations.

Treat these examples as evidence of a pipeline corruption bug.

========================================================
2. REPRODUCE THE BUG BEFORE FIXING IT
=====================================

First inspect the repository and identify the exact pipeline:

AI response
↓
streaming
↓
response accumulation
↓
structured output parser
↓
markdown/code extraction
↓
JSON parsing
↓
file object creation
↓
serialization/deserialization
↓
workspace writer
↓
filesystem
↓
Next.js compiler
↓
Preview

Find every location where source code can be:

* parsed
* escaped
* decoded
* encoded
* sanitized
* normalized
* rendered
* converted to HTML
* converted to Markdown
* converted to URLs
* serialized as JSON
* deserialized from JSON
* streamed
* concatenated
* regex processed
* transformed by WebView/UI code

Do NOT modify anything until this path is understood.

========================================================
3. CRITICAL RULE: SOURCE CODE IS OPAQUE DATA
============================================

This is the most important requirement.

Once the AI produces file content, CubicLM must treat file content as OPAQUE TEXT.

The application must NOT interpret JSX as:

HTML
Markdown
DOM
URL
UI text
Rich text
Template HTML
Browser content

For example:

The following must survive EXACTLY:

<div className="hero">
  <h1>Hello</h1>
</div>

The following must survive EXACTLY:

const id = `g-${a.slice(1)}-${b.slice(1)}`;

The following must survive EXACTLY:

href="#contact"

The following must survive EXACTLY:

{children}

The following must survive EXACTLY:

<> <Navigation /> <Hero />
</>

No UI renderer, WebView, Markdown renderer, URL resolver, HTML sanitizer, or text formatter is allowed to modify file contents.

========================================================
4. SEARCH FOR THESE BUG PATTERNS
================================

Search the entire CubicLM source code for:

* encodeURIComponent
* decodeURIComponent
* URL
* URLSearchParams
* escape
* unescape
* HTML escape
* HTML entity
* Markdown parser
* Markdown renderer
* DOMParser
* innerHTML
* textContent
* sanitize
* sanitizeHtml
* marked
* markdown
* regex replacing "<"
* regex replacing ">"
* regex replacing "{"
* regex replacing "}"
* regex replacing "`"
* regex replacing quotes
* replaceAll
* replace("<"
* replace(">"
* JSON.stringify
* JSON.parse
* URI encoding
* percent encoding
* link normalization
* localhost URL rewriting
* WebView navigation interception
* anchor rewriting
* HTML preprocessing

Do not assume these are bugs.

Determine whether any of them touches generated source files.

========================================================
5. PARTICULARLY INVESTIGATE URL REWRITING
=========================================

The generated source contains:

href="#contact"

but the resulting file contains something similar to:

http://127.0.0.1:39223/%22#contact%22

This is a huge clue.

Find the code responsible for transforming:

#contact

into:

http://127.0.0.1:<port>/...

The Preview WebView may have a navigation interceptor or URL resolver.

That logic MUST NEVER modify the source files on disk.

Separate:

SOURCE FILE PROCESSING

from:

PREVIEW URL PROCESSING

They must be completely independent.

A source file containing:

href="#contact"

must remain:

href="#contact"

even when Preview is running at:

http://127.0.0.1:39223

========================================================
6. INVESTIGATE TEMPLATE-LITERAL CORRUPTION
==========================================

The corruption:

const id = g-${a.slice(1)}-${b.slice(1)};

instead of:

const id = `g-${a.slice(1)}-${b.slice(1)}`;

suggests backticks may be stripped or interpreted by an intermediate parser.

Find exactly where backticks disappear.

Test:

const x = `hello`;

const x = `${value}`;

const x = `g-${a}-${b}`;

All must survive unchanged.

Do NOT solve this with a regex that simply adds backticks afterward.

Fix the underlying serialization/parsing problem.

========================================================
7. INVESTIGATE JSX TAG LOSS
===========================

The generated files preserve visible text such as:

Alex Morgan
About Me
Hello! I'm Alex.

but JSX element structures disappear.

This suggests that somewhere source code may be passing through an HTML/Markdown/text rendering layer.

Find where:

<Navigation />
<Hero />
<section>
<div>
<span>
<button>
<form>

are being interpreted or removed.

Test source such as:

return (

  <div className="test">
    <h1>Hello</h1>
    <button onClick={() => alert('hi')}>
      Click
    </button>
  </div>
);

The resulting file must be byte-for-byte equivalent.

========================================================
8. INVESTIGATE JSON TRANSPORT
=============================

If AI responses are transported as JSON:

DO NOT manually construct JSON using string concatenation.

BAD:

"{"path":"" + path + "","content":"" + content + ""}"

Use a real JSON serializer.

Correct architecture:

File object
{
path: string
content: string
}

↓
JSON serializer

↓
transport

↓
JSON parser

↓
File object

At no point should the content be:

HTML encoded
URL encoded
Markdown rendered
escaped twice
decoded twice

========================================================
9. STREAMING MUST BE LOSSLESS
=============================

If AI output is streamed:

DO NOT parse each chunk independently as a complete file.

BAD:

chunk 1 → parser
chunk 2 → parser
chunk 3 → parser

This can break:

* JSX
* JSON
* strings
* template literals
* escaped characters

Instead:

stream chunks
↓
append raw bytes/text
↓
complete response
↓
parse once
↓
validate
↓
write files

Handle UTF-8 boundaries correctly.

Never split a multibyte UTF-8 character incorrectly.

========================================================
10. USE STRUCTURED FILE OUTPUT
==============================

Preferred AI generation format:

{
"files": [
{
"path": "app/page.jsx",
"content": "..."
},
{
"path": "app/layout.jsx",
"content": "..."
}
]
}

The `content` value must be treated as raw source text.

Do not run Markdown parsing over it.

Do not run HTML parsing over it.

Do not run URL normalization over it.

Do not run UI formatting over it.

========================================================
11. REMOVE FRAGILE MARKDOWN EXTRACTION
======================================

If the current implementation expects:

```jsx
...source...
```

and extracts it using simplistic regex:

````regex
```(.*?)```
````

replace it with a robust parser or preferably structured output.

If Markdown fallback is still required:

* correctly identify language
* correctly preserve all content
* support multiline files
* support nested backticks where applicable
* do not trim meaningful whitespace
* do not modify source characters
* do not decode source as HTML

========================================================
12. FILE WRITER MUST BE LOSSLESS
================================

The workspace writer must write:

content

exactly as received.

No:

trim()
normalize()
HTML decode
URL decode
URL encode
Markdown conversion
line replacement
character substitution

unless explicitly required and proven safe.

Be especially careful with:

` " ' < > { } [ ] ( ) # $ % & \

and newline characters.

========================================================
13. ADD A SOURCE INTEGRITY LAYER
================================

Before writing:

originalContent

After writing:

readBackContent

Compare them.

For every generated file:

hash(originalContent)
hash(readBackContent)

They must match.

If they don't:

STOP.

Show:

"File write integrity check failed"

Do not continue to Preview.

This will tell us whether corruption happens:

BEFORE FILE WRITE

or

DURING FILE WRITE.

========================================================
14. ADD PIPELINE CHECKPOINT LOGGING
===================================

For development/debug builds, add optional diagnostics:

CHECKPOINT 1
Raw AI response

CHECKPOINT 2
Parsed file content

CHECKPOINT 3
Content immediately before workspace write

CHECKPOINT 4
Content immediately after filesystem write

CHECKPOINT 5
Content read back from filesystem

CHECKPOINT 6
Next.js compiler input/error

Do NOT log secrets/API keys.

For a suspicious file such as:

app/page.jsx

log:

path
length
line count
SHA-256 hash

not necessarily the entire source.

This lets us identify the exact corruption stage.

========================================================
15. ADD AUTOMATED LOSSLESS TRANSPORT TESTS
==========================================

Create tests containing difficult source strings.

TEST 1:

const x = `hello`;

TEST 2:

const x = `${name}`;

TEST 3:

const id = `g-${a.slice(1)}-${b.slice(1)}`;

TEST 4:

return (
<> <Navigation /> <Hero />
</>
);

TEST 5:

return (

  <div className="hero">
    <h1>Hello</h1>
  </div>
);

TEST 6:

href="#contact"

TEST 7:

const url = "http://127.0.0.1:3000";

TEST 8:

{children}

TEST 9:

'use client';

TEST 10:

<div>{condition ? <A /> : <B />}</div>

For every test:

AI/raw content
→ parser
→ serializer
→ writer
→ read-back

must produce EXACTLY the same content.

========================================================
16. NEXT.JS VALIDATION
======================

After source integrity passes:

Run the actual Next.js validation.

At minimum:

npm install

then:

npm run build

or development startup when appropriate.

Do not use Preview as the validator.

The compiler is the validator.

If:

npm run build

fails:

show the actual error.

========================================================
17. NEXT.JS RUNTIME
===================

Once source integrity is fixed, ensure the existing Next.js runtime pipeline supports:

Node.js
↓
npm
↓
next dev
↓
localhost
↓
Preview WebView

Do not assume Android has Node.js.

Use CubicLM's managed Node runtime if available.

If local Node cannot run:

use the existing cloud runtime/fallback architecture.

========================================================
18. PREVIEW MUST BE READ-ONLY WITH RESPECT TO SOURCE
====================================================

Preview can:

* load localhost
* intercept navigation
* handle external links
* refresh
* inspect errors

Preview must NEVER:

* rewrite files
* rewrite JSX
* rewrite href attributes in source
* URL encode source
* modify source files
* repair source files silently

Preview is a consumer of the project, not a source-code transformer.

========================================================
19. SEPARATE THESE THREE SYSTEMS
================================

They must be independent:

A. Source Pipeline

AI
→ parser
→ workspace

B. Runtime Pipeline

workspace
→ Node
→ npm
→ Next.js

C. Preview Pipeline

localhost
→ WebView

No Preview-specific code should execute while writing source files.

========================================================
20. NEXT.JS PROJECT FIX
=======================

After fixing the generic source pipeline, implement a Next.js runtime adapter.

Detect:

package.json → next dependency

Support:

App Router
Pages Router
JS
JSX
TS
TSX

For App Router:

app/page.*
app/layout.*

For Pages Router:

pages/index.*

Preserve:

'use client';

Server Components
Client Components
metadata
CSS imports
Next.js routing
Next.js assets

========================================================
21. DO NOT HARD-CODE THE CURRENT PROJECT
========================================

Do not add code like:

if (projectName === "portfolio-site") ...

Do not search for:

Alex Morgan

Do not repair:

app/page.jsx

with special rules.

The fix must work for arbitrary Next.js projects.

========================================================
22. TEST MULTIPLE PROJECT SHAPES
================================

Test at least:

A. Minimal Next.js

app/page.jsx

B. Multi-component Next.js

app/
├── page.jsx
├── layout.jsx
└── components/

C. Next.js with template literals

D. Next.js with anchor links

E. Next.js with client components

F. Next.js with SVG JSX

G. Next.js with forms/event handlers

H. Next.js with TypeScript

I. Next.js with CSS

J. Next.js with images/public assets

========================================================
23. REACT/VITE REGRESSION TEST
==============================

The existing React/Vite implementation currently works.

After your changes, verify:

React/Vite generation
→ npm install
→ vite dev
→ localhost
→ Preview

still works.

Do NOT replace the generic pipeline with a Next-only implementation.

========================================================
24. FAILURE MUST STOP THE PIPELINE
==================================

If any of these fail:

AI response parsing
file extraction
source integrity
filesystem write
read-back verification
package.json validation
Next.js build
server startup

then Preview must NOT claim success.

Example:

Source validation failed

app/page.jsx was modified during generation.

[View Error]
[Retry Generation]
[Ask AI to Fix]

========================================================
25. IMPORTANT: DO NOT "FIX" CORRUPTED SOURCE AFTER THE FACT
===========================================================

Do NOT create a generic post-processing step like:

* add missing JSX tags
* add missing backticks
* restore quotes
* convert URLs
* reconstruct components

That is dangerous.

The source should arrive correctly and remain unchanged.

Fix the stage that corrupts it.

========================================================
26. ARCHITECTURE
================

Implement a clean architecture similar to:

AI Response
│
▼
ResponseAccumulator
│
▼
StructuredFileParser
│
▼
SourceIntegrityValidator
│
▼
WorkspaceWriter
│
▼
Filesystem
│
├───────────────┐
▼               ▼
FrameworkDetector  FileExplorer
│
▼
RuntimeManager
│
▼
NodeRuntime
│
▼
Npm
│
▼
NextRuntime
│
▼
LocalServerManager
│
▼
127.0.0.1:<dynamic-port>
│
▼
Preview WebView

========================================================
27. ACCEPTANCE TEST
===================

The following MUST succeed:

User:

New Project
→ Next.js
→ Generate

CubicLM:

Generate source
✓
Parse files
✓
Validate files
✓
Write files
✓
Read-back integrity
✓
Detect Next.js
✓
Prepare Node
✓
Install dependencies
✓
Run Next.js
✓
Detect dynamic port
✓
Open Preview
✓

Preview displays the actual Next.js application.

Then:

AI modifies app/page.jsx

↓
file integrity passes

↓
Next.js Fast Refresh

↓
Preview updates

No source corruption.

========================================================
28. FINAL REPORT
================

Do not simply say "fixed".

Report:

1. Exact root cause
2. Exact file/function responsible
3. Why React/Vite was not affected
4. Why Next.js was affected
5. Where JSX tags were disappearing
6. Where backticks were disappearing
7. Where #anchor URLs were being rewritten
8. What was changed
9. How file integrity is verified
10. How streaming is handled
11. How JSON serialization is handled
12. How Next.js runtime works
13. How Preview works
14. Test results
15. React/Vite regression results
16. Remaining limitations

MOST IMPORTANT:

Do not stop after making Preview display a Next.js page.

The actual success condition is:

VALID AI SOURCE
→ LOSSLESS FILE PIPELINE
→ VALID FILESYSTEM
→ SUCCESSFUL NEXT.JS BUILD/RUNTIME
→ REAL LOCAL SERVER
→ REAL PREVIEW

If the source itself is corrupted, the task is NOT complete.
